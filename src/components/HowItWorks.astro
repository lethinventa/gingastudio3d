---
const steps = [
  {
    number: '01',
    title: 'Escolha seus produtos favoritos',
    body: 'Navegue pelo catálogo e encontre os brindes que você precisa.',
  },
  {
    number: '02',
    title: 'Adicione ao carrinho de compras',
    body: 'Escolha a quantidade e as personalizações e adicione os itens ao carrinho.',
  },
  {
    number: '03',
    title: 'Envie o pedido pelo WhatsApp',
    body: 'Clique em finalizar pedido e sua mensagem será gerada automaticamente.',
  },
  {
    number: '05',
    title: 'Produção sob encomenda',
    body: 'Fabricamos seus brindes com cuidado e qualidade garantida.',
  },
];
---

<section id="como-funciona" class="w-full py-8 md:py-16 px-6 scroll-mt-24">
  <div class="max-w-7xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
    {steps.map(({ number, title, body }, i) => (
      <div
        class="how-card bg-creme rounded-3xl p-7 flex flex-col gap-8 cursor-default"
        style={`transition-delay: ${i * 100}ms`}
      >
        <span class="how-card__number text-sm font-light text-terra">{number}</span>
        <div class="flex flex-col gap-2">
          <h3 class="how-card__title font-bold text-grafite text-xl leading-snug">{title}</h3>
          <p class="how-card__body text-grafite/70 text-sm leading-relaxed">{body}</p>
        </div>
      </div>
    ))}
  </div>
</section>

<style>
  .how-card {
    opacity: 0;
    transform: translateY(24px) scale(0.97);
    transition:
      opacity 0.6s ease,
      transform 0.6s cubic-bezier(0.22, 1, 0.36, 1),
      background-color 0.3s ease;
  }

  .how-card.is-visible {
    opacity: 1;
    transform: translateY(0) scale(1);
  }

  .how-card:hover {
    background-color: var(--color-azul);
    transform: rotate(2deg);
  }

  .how-card__number,
  .how-card__title,
  .how-card__body {
    transition: color 0.3s ease;
  }

  .how-card:hover .how-card__number {
    color: color-mix(in oklch, white 50%, transparent);
  }

  .how-card:hover .how-card__title {
    color: white;
  }

  .how-card:hover .how-card__body {
    color: color-mix(in oklch, white 70%, transparent);
  }

  @media (prefers-reduced-motion: reduce) {
    .how-card {
      opacity: 1;
      transform: none;
      transition: none;
    }

    .how-card:hover {
      transform: none;
    }
  }
</style>

<script>
  const cards = document.querySelectorAll('.how-card');

  const observer = new IntersectionObserver(
    (entries) => {
      for (const entry of entries) {
        if (entry.isIntersecting) {
          entry.target.classList.add('is-visible');
          observer.unobserve(entry.target);
        }
      }
    },
    { threshold: 0.2 }
  );

  cards.forEach((card) => observer.observe(card));
</script>
